modelVersion: "1.0"

source-id: "type"

name:
  ru: Тип данных
  en: Data type

controller:
  type: json
  config:
    metaFields:
      dependencies:
        - parent?str!"emodel/type@base"
        - parentRef?str!"emodel/type@base"
        - associations.target[]?str



