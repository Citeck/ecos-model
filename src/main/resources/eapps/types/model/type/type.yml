modelVersion: "1.0"

sourceId: "type"

name:
  ru: Тип данных
  en: Data type

controller:
  type: json
  config:
    format: yaml
    metaFields:
      dependencies:
        - parent?str!"emodel/type@base"
        - parentRef?str!"emodel/type@base"
